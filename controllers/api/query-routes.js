const router = require('express').Router();

router.get('/', (req, res) => {
    //return all previous queries
});

router.post('/', (req, res) => {
    // run query and return result
});

module.exports = router;